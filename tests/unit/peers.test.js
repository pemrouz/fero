!async function(cases){
  const { emitterify, key } = require('utilise/pure')
      , { same, equal, plan } = require('tap')
      , Peers = require('../../peers')
      , peers = new Peers({ cache: emitterify() })

    // TODO

}()